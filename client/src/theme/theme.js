import { createMuiTheme } from '@material-ui/core/styles';

export default createMuiTheme({
  palette: {
    primary: {
      main: "#18FFFF",
      contrastText: '#FFFFFF'
    },
    secondary: {
      main: "#FFFFFF",
      contrastText: "#FFFFFF"
    }
  },
});
